decoWallPainting4.png

Idle_dA		; animation name
1		; animation ID code
0,0 		; animation frame start position
128,256		; single frame size
128,256		; advancement step ( usually frame size )
1,1		; number of rows and cols of frames
1		; total number of frames ( usually rows*cols )
1000		; frame change speed ( millis )

Idle_dB		; animation name
2		; animation ID code
0,256 		; animation frame start position
128,256		; single frame size
128,256		; advancement step ( usually frame size )
1,1		; number of rows and cols of frames
1		; total number of frames ( usually rows*cols )
1000		; frame change speed ( millis )

#		; End of File Mark

